apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-account-opening-api.fullname" . }}
  labels:
    {{- include "digital-account-opening-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digital-account-opening-api.selectorLabels" . | nindent 4 }}
